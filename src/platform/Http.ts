export class Http {
  static async get (url: string): Promise<string | object> {
    // Use logger to document 
    // Use Exist to keep track of pending request and check if a new request can be sent
    return '';
  }
}