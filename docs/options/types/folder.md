Might be absolute or relative path to [[cwd]].
The folder **must** exist (i.e. the folder exists and the process can read content and write in it).

Exceptions :

* [[cwd]] can be relative to current working directory
* [[webapp]] *may* not exist
